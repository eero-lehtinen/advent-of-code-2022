{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Rust Build",
			"type": "shell",
			"command": "cargo",
			"args": ["build", "--bin", "${fileBasenameNoExtension}"],
			"options": {
				"cwd": "${fileDirname}/.."
			}
		}
	]
}
